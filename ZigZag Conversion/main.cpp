#include <iostream>
#include <string>
#include <vector>
using namespace std;

class Solution {
public:
    string convert(string s, int numRows) {
        string newString = "";
        int n = s.length();
        int mainCol = num-1;
        int mainRow = num - 2;
        for(int i = 0; i<n; i++){
            while(i <= i + mainCol)
        }
    }
};

int main(){
    string s = "PAYPALISHIRING";
    Solution solution ;
    cout << solution.convert(s,3);d
    return 0;
}